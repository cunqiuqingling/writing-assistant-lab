{
  "name": "writing-assistant-lab",
  "version": "0.8.0-m2",
  "private": true,
  "description": "Local-first English writing practice with editable document structure, folder-based long-text progress, BYOK reference analysis, and browser-side EPUB, DOCX and PDF import.",
  "scripts": {
    "vendor": "node scripts/vendor-assets.mjs",
    "build:site": "node scripts/build-site.mjs",
    "build:site:full": "npm run vendor && npm run build:site",
    "build:worker": "node scripts/build-worker.mjs",
    "dev": "npm run build:site:full && wrangler dev",
    "deploy": "npm run build:site:full && wrangler deploy",
    "check:m2": "node scripts/check-m2.mjs"
  },
  "dependencies": {
    "jszip": "3.10.1",
    "mammoth": "1.12.0",
    "pdfjs-dist": "6.1.200"
  },
  "devDependencies": {
    "wrangler": "4.114.0"
  }
}
